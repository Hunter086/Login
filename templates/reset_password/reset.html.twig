{% extends 'base.html.twig' %}

{% block title %}Reset your password{% endblock %}

{% block body %}
<link href="{{asset('css/login.css')}}" rel="stylesheet" type="text/css">

    <div class="container">
        <div class="card card-login mx-auto text-center bg-secondary ">
            <div class="card-header mx-auto bg-secondary">
                 <span> <img src="{{asset('img/pngwing.com.png')}}" class="w-75" alt="Logo"> </span><br/>
                                    <span class="logo_title mt-5"> Reset your password </span>
                                    {{ form_start(resetForm) }}
                        {{ form_row(resetForm.plainPassword) }}
                         
                        <button type="submit" name="btn" value="Login" class="btn btn-outline-danger float-right login_btn">Reset password</button>
                    {{ form_end(resetForm) }}
            </div>
        </div>
    </div>
    
{% endblock %}
